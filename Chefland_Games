#include <stdio.h>

int main(void) {
	// your code goes here
	int t, a, b, c, d;
	scanf("%d", &t);
	while(t--){
	    scanf("%d %d %d %d", &a, &b, &c, &d);
	    if(a==1 || b==1 || c==1 || d==1)
	    printf("out\n");
	    else
	    printf("in\n");
	}
	return 0;
}

