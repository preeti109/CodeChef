#include <stdio.h>
int main(void) {
	int t, x, y;
	scanf("%d", &t);
	while(t--){
	    scanf("%d %d", &x, &y);
	    if((x*y)%4==0)
	    printf("%d\n", (x*y)/4);
	    else
	    printf("%d\n", ((x*y)/4)+1);
	}
	return 0;
}

