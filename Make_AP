#include <stdio.h>

int main(void) {
	// your code goes here
	int t, x, z;
	scanf("%d", &t);
	while(t--){
	    scanf("%d %d", &x, &z);
	    if((x+z)%2 == 0)
	    printf("%d\n", (x+z)/2);
        else 
	    printf("-1\n");
    }
	return 0;
}

