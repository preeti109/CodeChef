#include <stdio.h>
int main(void) {
	int t, a, b;
	scanf("%d", &t);
	while(t--){
	    scanf("%d %d", &a, &b);
	    if((b*100/a)>=50)
	    printf("yes\n");
	    else
	    printf("no\n");
    }
	return 0;
}

