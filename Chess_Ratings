#include <stdio.h>

int main(void) {
	int t, x, y, z;
	scanf("%d",&t);
	while(t--) {
	    scanf("%d %d", &x, &y); 
	    z=y-x;
	    if((z%8)==0)
	    printf("%d\n", z/8);
	    else 
	    printf("%d\n", z/8 + 1);
	}
	return 0;
}

