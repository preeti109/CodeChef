#include <stdio.h>
int main(void) {
	int t, x, y, z;
	scanf("%d", &t);
	while(t--){
	    scanf("%d %d %d", &x, &y, &z);
	    if(x%(y*z)==0)
	        printf("%d\n",x/(y*z));
	    else
	        printf("%d\n",(x/(y*z))+1);
	}
	return 0;
}

