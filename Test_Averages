#include <stdio.h>
int main(void) {
	int t, a, b, c;
	scanf("%d", &t);
	while(t--){
	    scanf("%d %d %d", &a, &b, &c);
	    if((a+b)/2>=35 && (b+c)/2>=35 && (c+a)/2>=35)
            printf("pass\n");
        else
        printf("fail\n");
	}
	return 0;
}

