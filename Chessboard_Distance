#include <stdio.h>
int main(void) {
	int t, w, x, y, z;
	scanf("%d", &t);
	while(t--){
	    scanf("%d %d %d %d", &w, &x, &y, &z);
	    int a=w-y, b=x-z;
	    if(a<0)
	    a=a*(-1);
	    if(b<0)
	    b=b*(-1);
	    if(a<b)
	    printf("%d\n", b);
	    else
	    printf("%d\n", a);
	}
	return 0;
}

