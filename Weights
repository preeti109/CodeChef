#include <stdio.h>

int main(void) {
	// your code goes here
	int t, a, b, c, d;
	scanf("%d", &t);
	while(t--){
	    scanf("%d %d %d %d", &a, &b, &c, &d);
	    if(b+c==a || b+d==a || c+d==a || b+c+d==a || a==b || a==c || a==d)
	    printf("yes\n");
	    else
	    printf("no\n");
	}
	return 0;
}

