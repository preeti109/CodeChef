#include <stdio.h>
int main(void) {
int t, a, b;
scanf("%d", &t);
while(t--){
    scanf("%d %d", &a, &b);
    if((a+b+1)%4==1 || (a+b+1)%4==2)
        printf("Alice\n");
    else
        printf("Bob\n");
    }
	return 0;
}

