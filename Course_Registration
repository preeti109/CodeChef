#include <stdio.h>
int main(void) {
	int t;
	scanf("%d", &t);
	while(t--){
	    int x, y, z;
	    scanf("%d %d %d", &x, &y, &z);
	    if(x+z<=y){
	        printf("yes\n");
	    }else{
	        printf("no\n");
	    }
	}
	return 0;
}
