#include <stdio.h>

int main(void) {
	// your code goes here
	int t, x, y, z;
	scanf("%d", &t);
	while(t--){
	    scanf("%d %d %d", &x, &y, &z);
	    if((x*z) < y){
            printf("yes\n");
        }
        else if((x*z) >= y){
            printf("no\n");
        }
	}
	return 0;
}

