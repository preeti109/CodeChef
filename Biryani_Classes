#include<stdio.h>
int main()
{
    int t, i, weeks, amount, totalamount;
    scanf("%d",&t);
    for(i=0;i<t;i++){
        scanf("%d %d",&weeks,&amount);
        totalamount=weeks*amount;
        printf("%d\n",totalamount);
    }
    return 0;
}
