#include <stdio.h>
int main(void) {
	int t, x, y;
	scanf("%d", &t);
	while(t--){
	    int x, y;
	    scanf("%d %d", &x, &y);
	    if(x*10 == y*5)
	    printf("any\n");
	    else if(x*10 > y*5)
	    printf("first\n");
	    else 
	    printf("second\n");
	}
	return 0;
}

