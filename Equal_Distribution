#include <stdio.h>
int main(void) {
	int t;
	scanf("%d", &t);
	while(t--){
	    int x, y;
	    scanf("%d %d", &x, &y);
	    if((x+y)%2==0){
	        printf("yes\n");
	    }else{
	        printf("no\n");
	    }
	}
	return 0;
}
