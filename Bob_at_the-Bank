#include <stdio.h>

int main(void) {
	// your code goes here
	int t, p, q, r, s;
	scanf("%d", &t);
	while(t--){
	    scanf("%d %d %d %d", &p, &q, &r, &s);
	    printf("%d\n", ((q-r)*s)+p);
	}
	return 0;
}

