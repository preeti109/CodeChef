#include <stdio.h>

int main(void) {
	// your code goes here
	int t;
	float x, y, z;
	scanf("%d", &t);
	while(t--){
	    scanf("%f %f %f", &x, &y, &z);
	    if(((x+y)/2) > z){
	        printf("yes\n");
	    }
	    else{
	        printf("no\n");
	    }
	}
	return 0;
}

