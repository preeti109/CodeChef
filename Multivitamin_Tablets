#include <stdio.h>
int main(void){
	int t;
	scanf("%d", &t);
	while(t--){
	    int a, b;
	    scanf("%d %d", &a, &b);
	    if(a*3<=b){
	        printf("yes\n");
	    }else{
	        printf("no\n");
	    }
	}
   return 0;
}

