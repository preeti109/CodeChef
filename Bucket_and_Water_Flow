#include <stdio.h>

int main(void) {
	// your code goes here
	int t;
	scanf("%d", &t);
	while(t--){
	    int w, x, y, z;
	    scanf("%d %d %d %d", &w, &x, &y, &z);
	    if((y*z)+w > x){
	        printf("overflow\n");
	    }else if((y*z)+w < x){
	        printf("unfilled\n");
	    }else{
	        printf("filled\n");
	    }
	}
	return 0;
}

