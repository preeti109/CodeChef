#include <stdio.h>

int main(void) {
	// your code goes here
	int t;
	scanf("%d", &t);
	while(t--){
	    int x, y;
	    scanf("%d %d", &x, &y);
	    if(x < y){
	        printf("repair\n");
	    }else if(x == y){
	        printf("any\n");
	    }else{
	        printf("new phone\n");
	    }
	}
	return 0;
}

