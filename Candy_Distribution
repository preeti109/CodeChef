#include <stdio.h>

int main(void) {
	// your code goes here
	int t, x, y;
	scanf("%d", &t);
	while(t--){
	    scanf("%d %d", &x, &y);
	    if((x/y)%2==0 && (x/y)*y==x)
	    printf("yes\n");
	    else
	    printf("no\n");
	}
	
	return 0;
}

