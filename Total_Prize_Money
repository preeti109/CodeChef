#include <stdio.h>

int main(void) {
	// your code goes here
	int t;
	scanf("%d", &t);
	while(t--){
	    int x, y, p, q, r;
	    scanf("%d %d", &x, &y);
	    for(int i=1; i<=100; i++){
	        p=10*x;
	        q=90*y;
	    }
	r=p+q;
	printf("%d\n", r);
	}
	return 0;
}

