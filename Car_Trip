#include <stdio.h>
int main(void){
	int t;
	scanf("%d", &t);
	while(t--){
	    int a;
	    scanf("%d", &a);
	    if(a<300){
	        printf("3000\n");
	    }else if(a>=300){
	        printf("%d\n", a*10);
	    }
	}
   return 0;
}

