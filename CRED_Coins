#include <stdio.h>
int main(void) {
	int t, x, y;
	scanf("%d", &t);
	while(t--){
	    scanf("%d %d", &x, &y);
	    if(x*y >= 100)
	    printf("%d\n", (x*y)/100);
	    else
	    printf("0\n");
	}
	return 0;
}

