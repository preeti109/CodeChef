#include <stdio.h>

int main(void) {
	// your code goes here
	int t, x, y;
	scanf("%d", &t);
	while(t--){
	    scanf("%d %d", &x, &y);
	    if(y>=x)
	    printf("%d\n", x);
	    else 
	    printf("%d\n", 2*x-y);
	}
	return 0;
}

