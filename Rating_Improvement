#include <stdio.h>

int main(void) {
	// your code goes here
	int t;
	scanf("%d", &t);
	while(t--){
	    int x, y;
	    scanf("%d %d", &x, &y);
	    if(x+200>=y && y>=x){
	        printf("yes\n");
	    }else{
	        printf("no\n");
	    }
	}
	return 0;
}

