#include <stdio.h>

int main(void) {
	// your code goes here
	int t, w, x, y, z;
	scanf("%d", &t);
	while(t--){
	    scanf("%d %d %d %d", &w, &x, &y, &z);
	    if((w*x)<=(y*z))
	    printf("yes\n");
	    else
	    printf("no\n");
	}
	return 0;
}

