#include <stdio.h>
int main(void) {
	int t;
	scanf("%d", &t);
	while(t--){
	    int x;
	    scanf("%d", &x);
	    if(x>=4 && x<7){
            printf("medium\n");
	    }else if(x<4){
	        printf("mild\n");
	    }else{
	        printf("hot\n");
	    }
    }
	return 0;
}

