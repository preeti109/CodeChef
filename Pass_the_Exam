#include<stdio.h>
void main(){
	int n;
	scanf("%d", &n);
	while(n--){
		int a, b, c;
		scanf("%d %d %d", &a, &b, &c);
		if(a+b+c>=100 && a>=10 && b>=10 && c>=10){
		    printf("pass\n");
		}else{
		    printf("fail\n");
		}
		
	}
}
