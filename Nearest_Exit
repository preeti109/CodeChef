#include <stdio.h>

int main(void) {
	// your code goes here
	int t, x;
	scanf("%d", &t);
	while(t--){
	    scanf("%d", &x);
	    if(x>=1 && x<=50)
        printf("LEFT\n");
        else if(x>50 && x<=100)
        printf("RIGHT \n");
	}
	return 0;
}

